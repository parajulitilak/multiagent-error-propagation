% =========================================================================
% Which Verifier Catches What? Mapping Error Severity to Verifier Strategy
% in Multi-Agent LLM Pipelines
% SISTER 2026, AI/ML Track (Dubai CS Society + Delta Rising Foundation)
%
% Self-contained: compiles on Overleaf as-is (pdfLaTeX). Structure follows
% the SISTER research template: I Introduction, II Methods, III Results,
% IV Analysis, V Conclusion, VI Improvements, VII Evaluation, References,
% with author-year citations and a source link on every reference.
%
% Convention: \pending{...} marks every number or claim that must come from
% our own runs. Nothing pending may survive into a submitted version.
% =========================================================================
\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc}
\usepackage{microtype} % NOTE: if a local build errors on font expansion, use [expansion=false]
\usepackage[colorlinks=true, citecolor=blue, urlcolor=blue, linkcolor=blue]{hyperref}
\usepackage{caption}
\captionsetup{labelsep=period,font=small}
\usepackage[numbers,square,sort&compress]{natbib}
\usepackage{xcolor}
\newcommand{\pending}[1]{\textcolor{purple}{[#1]}}
\renewcommand{\thesection}{\Roman{section}}

\title{\bf Which Verifier Catches What? Mapping Error Severity\\to Verifier Strategy in Multi-Agent LLM Pipelines}
\author{Tilak Parajuli\\
\small SISTER 2026, AI/ML Track, Dubai Computer Science Society \& Delta Rising Foundation\\
\small Kathmandu, Nepal
\and Johnny Kozman\\
\small SISTER 2026, AI/ML Track, Dubai Computer Science Society \& Delta Rising Foundation\\
\small Cairo, Egypt
\and Arushi Waddepalli\\
\small SISTER 2026, AI/ML Track, Dubai Computer Science Society \& Delta Rising Foundation\\
\small Hyderabad, India}
\date{August 2026}

\begin{document}
\maketitle

\begin{abstract}
Pipelines of specialized LLM agents (one plans, another executes, a third
verifies) are now a default design for applied language-model systems.
The design carries an implicit promise: later stages should absorb the
mistakes of earlier ones. Whether they actually do is unresolved. Failure
taxonomies built from deployed systems describe \emph{what} goes wrong but,
being observational, cannot say how often an early error survives to the
output or what a verification stage causally contributes. We address this
with controlled fault injection, adapted from software reliability
engineering: we corrupt a single intermediate output with a known, scripted
perturbation and trace its fate through the rest of the pipeline. Faults are
graded into three severity levels, and the verification stage is additionally
evaluated under three prompt strategies (direct, chain-of-thought, rubric)
replayed on identical traces. Across \pending{200} GSM8K problems
\pending{and 150 HotpotQA questions}, we compare a single-agent baseline, a
clean three-stage pipeline, early- and late-stage injection, and
verifier-removed ablations of both, analyzing outcomes with paired statistics
throughout. Injected faults reached the final answer in \pending{XX\%} of
cases; the verifier flagged \pending{YY\%} of faults but repaired only
\pending{ZZ\%}, and faults planted at the planning stage survived
\pending{significantly more often} than those planted during execution. The
result is a map of which verifier strategy catches which error type at which
severity \pending{(headline numbers pending)}. We release code, prompts, and
full traces.
\end{abstract}

\section{Introduction}

The unit of deployment for language models is shifting from a single model
call to a pipeline of agents. A decomposer agent plans the task, a solving
agent executes the plan, and a verification agent checks the result against the ground truth; frameworks such as AutoGen~\citep{wu2023autogen} and
MetaGPT~\citep{hong2023metagpt} have made this pattern easy to build and
correspondingly common. The pattern borrows its justification from real-time human
organizations: specialization should raise accuracy, and review should catch
what specialization misses.

The evidence for that justification is mixed. On one side, multi-agent debate
has been reported to improve factuality and reasoning~\citep{du2023debate},
and simple ensembling over agents continues to help as the number of agents
grows~\citep{li2024moreagents}. On the other, a growing body of work finds
that language models are poor judges of their own reasoning: without external
feedback, self-correction often leaves accuracy unchanged or makes it
worse~\citep{huang2023selfcorrect}. Between these positions sits the most
detailed account of multi-agent behavior to date, the Multi-Agent System Failure Taxonomy (MAST) ~\citep{cemri2025mast}, which catalogues fourteen failure modes
observed across seven deployed frameworks. MAST establishes that inter-agent
failures are frequent and varied. But it is an observational study: when a deployed system fails, the logs show the outcome but not necessarily where the error began. Researchers must reconstruct the failure afterward, and logs alone cannot reveal how often an early-stage mistake would have persisted to the final output.

What is missing is an interventional measurement. If we could plant a known
error at a chosen stage and watch what the rest of the pipeline does with it,
questions that the observational work leaves open would become directly
answerable: How often does an early mistake reach the user? Does it matter
where in the pipeline the mistake occurs? Does it matter how blatant the
mistake is? How much of the damage does a verifier actually prevent, as
opposed to merely being present while the pipeline succeeds for other
reasons?

We make that measurement. Borrowing fault injection from software
reliability engineering, we corrupt exactly one intermediate artifact of a
Decomposer--Solver--Verifier pipeline (swapping a quantity or inverting an
arithmetic operation) and let the remaining stages run untouched. Because
the fault is planted by us, its origin is known by construction, and every
downstream outcome can be attributed. Each fault carries a pre-registered
severity level, and because every stage's output is stored, the verification
stage can be re-evaluated under alternative prompt strategies on identical
inputs. On fixed problem samples from GSM8K~\citep{cobbe2021gsm8k} and HotpotQA~\citep{yang2018hotpotqa}, we evaluate six conditions:
a single-agent baseline, the clean Decomposer--Solver--Verifier pipeline, injection at the planning stage,
injection at the solving stage, and verifier-removed variants of both
injected pipelines. All comparisons are paired, and we report exact tests and
bootstrap intervals rather than point estimates alone.

Our thesis is falsifiable: in a Decomposer--Solver--Verifier pipeline,
planted faults survive to the final answer at rates that depend on where they
are planted, how severe they are, and how the verifier is prompted, and the
verification stage prevents only part of the resulting damage. The six
pre-registered conditions below test each clause of that claim.

This study sits at the intersection of four bodies of work.
AutoGen~\citep{wu2023autogen}, MetaGPT~\citep{hong2023metagpt}, and
CAMEL~\citep{li2023camel} established conversational and assembly-line
patterns for composing LLM agents; the Decomposer--Solver--Verifier pipeline
we study is the smallest configuration that exhibits the planning, execution,
and review roles these frameworks popularized, and our contribution is not a
new framework but the measurement of a failure property this family of systems
shares. Second, the case that adding agents helps: debate improves factuality
on several benchmarks~\citep{du2023debate}, majority voting over sampled
agents scales favorably~\citep{li2024moreagents}, and self-consistency
achieves related gains within a single model~\citep{wang2022selfconsistency}.
These results measure aggregate accuracy; they do not distinguish a pipeline
that prevents errors from one that merely commits different ones, which is
precisely the distinction fault injection makes observable. Third,
self-correction and verification: Reflexion~\citep{shinn2023reflexion} and
Self-Refine~\citep{madaan2023selfrefine} show that models can revise their
outputs given feedback, while \citep{huang2023selfcorrect} demonstrate that
without external signals such revision often fails on reasoning tasks, and
LLM-as-judge evaluation carries known biases~\citep{zheng2023judge}; our
verifier ablation estimates, under identical inputs, how much a review stage
changes the fate of a known error. 

Finally, failure analysis:
MAST~\citep{cemri2025mast} provides the vocabulary (specification failures,
inter-agent misalignment, verification failures) and we adopt it, but where
MAST classifies failures that occurred in the wild, we induce failures under
control; the two are complementary in the way epidemiology and randomized
trials are. Mitigation systems are now being engineered against exactly this
phenomenon: DebUnc~\citep{yoffe2024debunc} attaches uncertainty metrics to inter-agent messages so
that unreliable ones can be discounted, and~\citep{xie2026spark} model error cascades on the agent dependency graph and
add a governance layer that suppresses them. Both presuppose that upstream
errors propagate downstream. Our study measures that presupposition directly
and quantifies how much protection a standard verifier stage already
provides.

\textbf{Contributions.} 

(i)~A controlled fault-injection methodology for
measuring error propagation in LLM agent pipelines, with scripted,
reproducible perturbation operators graded by severity.

(ii)~Quantitative propagation, catch, and correction rates, including the
effects of injection position and fault severity, under paired statistical
tests. 

(iii)~A verifier ablation that isolates the causal contribution of the
verification stage. 

(iv)~A strategy-level map of verification: catch and
correction rates for three verifier prompt strategies across error types and
severities, obtained by replaying identical stored traces. 

(v)~Code,
prompts, configuration, and complete traces, sufficient to regenerate every
number in this paper from one script.

\section{Methods}

\subsection{Pipeline under study}
Let $x$ be a problem. The pipeline computes a plan $d = D(x)$, a solution
$s = S(x, d)$, and a verdict $v = V(x, s)$; the final answer $\hat{y}$ is
extracted from $s$ if the verifier accepts, or from the verifier's correction
if it rejects. Each stage is a separate LLM call with its own role prompt
(Appendix~A); the only information passed between stages is the text of the
preceding artifact, mirroring common framework defaults. A single-agent
baseline computes $\hat{y}$ from one call on $x$ alone.

% Figure
\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    >=Latex,
    font=\sffamily,
    io/.style={
        draw=gray!70!black,
        fill=white,
        rounded corners=8pt,
        minimum width=2.5cm,
        minimum height=1.4cm,
        align=center,
        line width=0.9pt
    },
    stage/.style 2 args={
        draw=#1!70!black,
        fill=#1!85,
        rounded corners=10pt,
        minimum width=3.2cm,
        minimum height=2.3cm,
        align=center,
        text=white,
        font=\bfseries\Large,
        line width=1pt
    },
    artifact/.style={
        rounded corners=10pt,
        minimum width=3.5cm,
        minimum height=1.05cm,
        align=center,
        fill=white,
        line width=1pt,
        font=\large
    },
    inj/.style={
        circle,
        draw=red!85!black,
        dashed,
        line width=1.2pt,
        minimum size=13mm,
        inner sep=0pt
    },
    flow/.style={->, line width=1.2pt, draw=black!85},
    note/.style={font=\bfseries\large, text=red!85!black},
    dottedlink/.style={dotted, line width=1.4pt}
]

% Main nodes
\node[io] (problem) {Problem $x$};

\node[stage={teal}{}, right=1.8cm of problem] (decomp) {Decomposer};

\node[inj, right=1.1cm of decomp] (inj1) {};
\draw[red!85!black, line width=1.3pt]
    ($(inj1.center)+(0,0.34)$) --
    ($(inj1.center)+(0.11,0.07)$) --
    ($(inj1.center)+(0.01,0.07)$) --
    ($(inj1.center)+(0.09,-0.20)$) --
    ($(inj1.center)+(-0.10,-0.01)$) --
    ($(inj1.center)+(-0.01,-0.01)$) -- cycle;

\node[stage={orange}{}, right=1.1cm of inj1] (solver) {Solver};

\node[inj, right=1.1cm of solver] (inj2) {};
\draw[red!85!black, line width=1.3pt]
    ($(inj2.center)+(0,0.34)$) --
    ($(inj2.center)+(0.11,0.07)$) --
    ($(inj2.center)+(0.01,0.07)$) --
    ($(inj2.center)+(0.09,-0.20)$) --
    ($(inj2.center)+(-0.10,-0.01)$) --
    ($(inj2.center)+(-0.01,-0.01)$) -- cycle;

\node[stage={green!60!black}{}, right=1.1cm of inj2] (verifier) {Verifier};

\node[io, right=1.8cm of verifier] (answer) {Final answer $\hat{y}$};

% Arrows
\draw[flow] (problem) -- (decomp);
\draw[flow] (decomp) -- (inj1);
\draw[flow] (inj1) -- (solver);
\draw[flow] (solver) -- (inj2);
\draw[flow] (inj2) -- (verifier);
\draw[flow] (verifier) -- (answer);

% Artifact boxes
\node[artifact, draw=teal!70!black, below=1.45cm of decomp] (plan) {plan $d = D(x)$};
\node[artifact, draw=orange!80!black, below=1.45cm of solver] (soln) {solution $s = S(x,d)$};
\node[artifact, draw=green!50!black, below=1.45cm of verifier] (verd) {verdict $v = V(x,s)$};

\draw[dottedlink, teal!70!black] (decomp.south) -- (plan.north);
\draw[dottedlink, orange!80!black] (solver.south) -- (soln.north);
\draw[dottedlink, green!50!black] (verifier.south) -- (verd.north);

% Injection labels
\node[note, above=0.45cm of inj1] {C1 / D1};
\node[note, above=0.45cm of inj2] {C2 / D2};

% Legend
\node[
    draw=gray!60,
    rounded corners=4pt,
    fill=white,
    minimum width=5.8cm,
    minimum height=0.70cm,
    anchor=north east,
    font=\scriptsize
] at ($(answer.south east)+(0,-3.95)$) (legendbox) {};

\node[inj, minimum size=5mm] at ($(legendbox.west)+(0.45,0)$) (leginj) {};
\draw[red!85!black, line width=0.7pt]
    ($(leginj.center)+(0,0.14)$) --
    ($(leginj.center)+(0.05,0.03)$) --
    ($(leginj.center)+(0.01,0.03)$) --
    ($(leginj.center)+(0.04,-0.08)$) --
    ($(leginj.center)+(-0.05,-0.01)$) --
    ($(leginj.center)+(-0.01,-0.01)$) -- cycle;

\node[anchor=west, font=\normalsize] at ($(legendbox.west)+(0.85,0)$)
{= controlled fault injection};

\end{tikzpicture}
}
\caption{The Decomposer--Solver--Verifier pipeline with the two injection points marked.}
\label{fig:pipeline}
\end{figure}

\subsection{Controlled fault injection}
\label{sec:injection}
An injector $\phi$ maps an artifact to a corrupted artifact and a record of
the change. We use two operators chosen to mimic the arithmetic slips that
occur naturally in reasoning traces: \emph{number swap}, which replaces one
quantity with a plausible wrong value drawn from a severity-dependent
multiplier set, and \emph{operation flip}, which inverts one arithmetic
operation word (\emph{add}$\leftrightarrow$\emph{subtract},
\emph{multiply}$\leftrightarrow$\emph{divide}, and analogous pairs). List and
step labels are excluded from number swap: corrupting ``Step 1'' changes no
quantity, and admitting such perturbations would dilute the injected-fault
population with harmless cases and bias propagation rates downward. Every
injection logs the operator, the original and replacement spans, and the
character position, so each downstream error is attributable to a specific,
inspectable change.

Number swaps are graded into three pre-registered severity levels by the
relative size of the change: \emph{mild} (under a 15\% change),
\emph{moderate} (a 30--60\% change), and \emph{severe} (more than a 200\%
change or a sign flip). Severity levels are balanced across the fixed sample
and recorded with every injection, so propagation and detection can be
stratified by how visible a fault is. \pending{The exact multiplier set per
level is fixed in the released configuration before the main run.}

In condition C1 the injector is applied to the plan $d$ before the solver
sees it; in C2, to the solution $s$ before the verifier sees it. Exactly one
artifact is corrupted per run.

\subsection{Conditions and design}
\begin{table}[h]
\centering\small
\begin{tabular}{lccc}
\toprule
Condition & Pipeline & Injection & Verifier \\
\midrule
A  & --         & --          & --          \\
B  & \checkmark & --          & \checkmark  \\
C1 & \checkmark & Decomposer  & \checkmark  \\
C2 & \checkmark & Solver      & \checkmark  \\
D1 & \checkmark & Decomposer  & --          \\
D2 & \checkmark & Solver      & --          \\
\bottomrule
\end{tabular}
\caption{Experimental conditions. Every condition runs on the same
fixed problem sample (seeded), so all comparisons are paired at the problem
level.}
\label{tab:conditions}
\end{table}

\subsection{Verifier prompt strategies}
\label{sec:strategies}
The verifier is the pipeline's final stage, and every run stores the exact
problem, plan, and solution the verifier received. We exploit this to compare
three verifier prompt strategies on identical inputs by replaying the frozen
traces of conditions B, C1, and C2 rather than re-running the pipeline: V1,
the direct verifier used in the main conditions (Appendix~A); V2, a
chain-of-thought verifier instructed to re-derive the solution independently
before judging; and V3, a rubric verifier that checks an explicit list of
failure points. Because the upstream text is identical across strategies, any
difference in catch or correction rate is attributable to the verifier prompt
alone. \pending{V2 and V3 prompt texts to be finalized and released before
the replay runs.}

\subsection{Outcome measures}
For a problem the clean pipeline solves correctly, an injected fault has one
of four fates: \emph{caught and corrected} (verifier rejects; final answer
correct), \emph{caught but not corrected} (verifier rejects; final answer
wrong), \emph{silently propagated} (verifier accepts; final answer wrong), or
\emph{absorbed} (verifier accepts; final answer nevertheless correct, e.g.,
the solver ignored the corrupted span). We report: accuracy per condition;
\emph{propagation rate}, the fraction of valid injections whose final answer
is wrong, restricted to problems condition B solved correctly so that new
errors are attributable to the injection; \emph{catch rate} and
\emph{correction rate} of the verifier, overall and stratified by error type,
severity, and prompt strategy; and cost per solved problem in tokens.

\subsection{Statistical analysis}
All condition contrasts use McNemar's exact test on paired
correct/incorrect outcomes~\citep{dietterich1998approximate}, the appropriate
test when two systems answer the same items. Accuracies carry 95\%
percentile-bootstrap intervals (10{,}000 resamples); rates carry Wilson
score intervals. The effects of injection position, fault severity, and task
type on fault survival are estimated by logistic regression, reported as
odds ratios. Because several contrasts are tested, $p$-values are
Holm--Bonferroni corrected~\citep{dror2018hitchhiker}. Main runs use
temperature~0 on a fixed seeded sample; a robustness replication uses
temperature~0.7 over three seeds, reported as mean and interval.

\subsection{Setup and reproducibility}
Model: \pending{exact model string}, accessed \pending{dates}, temperature 0
(main), 0.7 (robustness). Datasets: GSM8K test split, $n=\pending{200}$
problems sampled with seed 42 \pending{; HotpotQA distractor setting,
$n=150$}. Injection seed 7; operators and severity levels as in
\S\ref{sec:injection}. Prompts are fixed across conditions and listed
verbatim in Appendix~A. Total API cost: \pending{\$XX}. All configuration is
a single YAML file in the released repository; \texttt{python -m src.stats}
regenerates every number below from the released traces.

\section{Results}
\pending{This section is written after the runs. Planned presentation:}

\textbf{Does the pipeline beat one agent?} Figure~\ref{fig:acc} compares
conditions A and B \pending{(bar chart, 95\% CIs; McNemar $p$)}.

\textbf{What happens to an injected fault?} Figure~\ref{fig:fate} gives the
error-fate breakdown for C1 and C2 \pending{(stacked bars or Sankey:
caught-and-corrected / caught-not-corrected / silently-propagated /
absorbed)}.

\textbf{Does position matter?} \pending{Propagation rate by injection stage
with odds ratio from the logistic model (RQ3).}

\textbf{Does severity matter?} \pending{Propagation, catch, and correction
rates stratified by severity level, with odds ratios from the logistic model
(RQ1).}

\textbf{What does the verifier contribute?} \pending{C1 vs.\ D1 and C2 vs.\ D2:
propagation with and without the verifier; the difference is the verifier's
causal contribution.}

\textbf{Which verifier catches what?} \pending{Catch and correction rates per
prompt strategy (V1/V2/V3) by error type and severity; summary map in
Table~\ref{tab:map} (RQ2, RQ3).}

\begin{figure}[t]\centering
\caption{Accuracy by condition with 95\% bootstrap intervals.
\pending{pending}}\label{fig:acc}
\end{figure}
\begin{figure}[t]\centering
\caption{Fate of injected faults by injection stage.
\pending{pending}}\label{fig:fate}
\end{figure}
\begin{table}[t]\centering
\caption{Catch rate by verifier strategy, error type, and severity.
\pending{pending}}\label{tab:map}
\end{table}

\section{Analysis}
\pending{Three short case studies drawn from traces: one silent propagation
(quote the corrupted span and the solver's uncritical use of it), one
catch-and-correct, one absorption. Each at most a paragraph; the point is
mechanism, not anecdote volume. Then: what the severity gradient and the
strategy map say about \emph{why} verifiers miss what they miss.}

\section{Conclusion}
\pending{One paragraph, written last: what the numbers established, one
sentence on what changes for practitioners (where to spend verification
budget and which verifier strategy to choose), one sentence on next steps.}

\paragraph{Broader impact.} Agent pipelines increasingly mediate consequential
work. Measuring where errors survive, and how much verification actually
helps, informs where guardrails belong; we do not foresee direct misuse
enabled by these measurements.

\paragraph{Reproducibility.} Code, prompts, configuration, and full traces:
\pending{repository URL}.

\section{Improvements}
Our perturbations are synthetic by design; this is what makes origin
attribution exact, but naturally arising errors are more varied, and our
operators cover arithmetic slips rather than, e.g., factual hallucinations.
Results are reported for \pending{one model family} and \pending{one to two}
task distributions with objectively checkable answers; propagation dynamics
on open-ended tasks may differ. The verifier prompts are representative
rather than exhaustively tuned, so our estimates of verification benefit
should be read as typical-configuration, not best-achievable. The study was
designed and executed within a four-week program.

Several extensions would strengthen the study, in rough order of value:
injecting semantic faults (contradictions, fabricated entities) to test
whether verification behaves differently on non-arithmetic errors;
replicating the full design on a second model family to separate
pipeline-level effects from model-level ones; tightening the solver prompt's
plan adherence, so that plan-stage faults are not absorbed by silent
re-derivation and the planning-stage estimate is sharpened; comparing a
small verifier fine-tuned with QLoRA~\citep{dettmers2023qlora} on the
released traces against the LLM judge, to test whether low-cost specialized
oversight can match it; and evaluating learned inter-agent gating in the
style of DebUnc~\citep{yoffe2024debunc} under our injection protocol, which
would connect the measurement in this paper to the mitigation literature it
motivates.

\section{Evaluation}
We assess the design against the standard threats to validity.
\emph{Internal validity}: all contrasts are paired on the same fixed,
seeded problem sample; every injection is validated (a swap must change the
value; labels are excluded) and its full record released, so any reader can
audit the treatment. \emph{Construct validity}: propagation is measured only
on problems the clean pipeline solves correctly, so a wrong final answer
under injection is attributable to the fault rather than to baseline
inability, and absorption is reported as its own fate rather than folded
into success. \emph{Statistical validity}: exact paired tests, Wilson and
bootstrap intervals, and Holm correction over the pre-registered contrast
family~\citep{dror2018hitchhiker}. \emph{External validity}: results cover
\pending{one to two} task distributions with objectively checkable answers
and \pending{one model family}; a robustness replication at temperature 0.7
across three seeds tests sensitivity to decoding randomness
\pending{(robustness numbers pending)}. Conclusions are stated only where
these checks agree.

\begin{thebibliography}{99}

\bibitem{cemri2025mast}
Mert Cemri, Melissa Z. Pan, Shuyi Yang, Lakshya A. Agrawal, Bhavya Chopra,
Rishabh Tiwari, Kurt Keutzer, Aditya Parameswaran, Dan Klein,
Kannan Ramchandran, Matei Zaharia, Joseph E. Gonzalez, and Ion Stoica.
\href{https://arxiv.org/abs/2503.13657}{\textcolor{blue}{Why Do Multi-Agent {LLM} Systems Fail?}}
\textit{arXiv preprint arXiv:2503.13657}, 2025.

\bibitem{wu2023autogen}
Qingyun Wu, Gagan Bansal, Jieyu Zhang, Yiran Wu, Beibin Li, Erkang Zhu,
Li Jiang, Xiaoyun Zhang, Shaokun Zhang, Jiale Liu, Ahmed Hassan Awadallah,
Ryen W. White, Doug Burger, and Chi Wang.
\href{https://arxiv.org/abs/2308.08155}{\textcolor{blue}{{AutoGen}: Enabling Next-Gen {LLM} Applications via Multi-Agent Conversation}}
\textit{arXiv preprint arXiv:2308.08155}, 2023.

\bibitem{hong2023metagpt}
Sirui Hong, Xiawu Zheng, Jonathan Chen, Yuheng Cheng, Jinlin Wang,
Ceyao Zhang, Zili Wang, Steven Ka Shing Yau, Zijuan Lin, Liyang Zhou,
Chenyu Ran, Lingfeng Xiao, Chenglin Wu, and J{\"u}rgen Schmidhuber.
\href{https://arxiv.org/abs/2308.00352}{\textcolor{blue}{{MetaGPT}: Meta Programming for a Multi-Agent Collaborative Framework}}
\textit{arXiv preprint arXiv:2308.00352}, 2023.

\bibitem{li2023camel}
Guohao Li, Hasan Hammoud, Hani Itani, Dmitrii Khizbullin, and Bernard Ghanem.
\href{https://arxiv.org/abs/2303.17760}{\textcolor{blue}{{CAMEL}: Communicative Agents for ``Mind'' Exploration of Large Language Model Society}}
\textit{Advances in Neural Information Processing Systems}, 36, 2023.

\bibitem{du2023debate}
Yilun Du, Shuang Li, Antonio Torralba, Joshua B. Tenenbaum, and Igor Mordatch.
\href{https://arxiv.org/abs/2305.14325}{\textcolor{blue}{Improving Factuality and Reasoning in Language Models through Multiagent Debate}}
\textit{arXiv preprint arXiv:2305.14325}, 2023.

\bibitem{li2024moreagents}
Junyou Li, Qin Zhang, Yangbin Yu, Qiang Fu, and Deheng Ye.
\href{https://arxiv.org/abs/2402.05120}{\textcolor{blue}{More Agents Is All You Need}}
\textit{arXiv preprint arXiv:2402.05120}, 2024.

\bibitem{huang2023selfcorrect}
Jie Huang, Xinyun Chen, Swaroop Mishra, Huaixiu Steven Zheng, Adams Wei Yu,
Xinying Song, and Denny Zhou.
\href{https://arxiv.org/abs/2310.01798}{\textcolor{blue}{Large Language Models Cannot Self-Correct Reasoning Yet}}
\textit{arXiv preprint arXiv:2310.01798}, 2023.

\bibitem{shinn2023reflexion}
Noah Shinn, Federico Cassano, Ashwin Gopinath, Karthik Narasimhan, and Shunyu Yao.
\href{https://arxiv.org/abs/2303.11366}{\textcolor{blue}{{Reflexion}: Language Agents with Verbal Reinforcement Learning}}
\textit{Advances in Neural Information Processing Systems}, 36, 2023.

\bibitem{madaan2023selfrefine}
Aman Madaan, Niket Tandon, Prakhar Gupta, Skyler Hallinan, Luyu Gao,
Sarah Wiegreffe, Uri Alon, Nouha Dziri, Shrimai Prabhumoye, Yiming Yang,
Shashank Gupta, Bodhisattwa Prasad Majumder, Katherine Hermann,
Sean Welleck, Amir Yazdanbakhsh, and Peter Clark.
\href{https://arxiv.org/abs/2303.17651}{\textcolor{blue}{{Self-Refine}: Iterative Refinement with Self-Feedback}}
\textit{Advances in Neural Information Processing Systems}, 36, 2023.

\bibitem{zheng2023judge}
Lianmin Zheng, Wei-Lin Chiang, Ying Sheng, Siyuan Zhuang, Zhanghao Wu,
Yonghao Zhuang, Zi Lin, Zhuohan Li, Dacheng Li, Eric Xing, Hao Zhang,
Joseph E. Gonzalez, and Ion Stoica.
\href{https://arxiv.org/abs/2306.05685}{\textcolor{blue}{Judging {LLM}-as-a-{Judge} with {MT-Bench} and {Chatbot Arena}}}
\textit{Advances in Neural Information Processing Systems}, 36, 2023.

\bibitem{wei2022cot}
Jason Wei, Xuezhi Wang, Dale Schuurmans, Maarten Bosma, Brian Ichter,
Fei Xia, Ed Chi, Quoc V. Le, and Denny Zhou.
\href{https://arxiv.org/abs/2201.11903}{\textcolor{blue}{Chain-of-Thought Prompting Elicits Reasoning in Large Language Models}}
\textit{Advances in Neural Information Processing Systems}, 35, 2022.

\bibitem{wang2022selfconsistency}
Xuezhi Wang, Jason Wei, Dale Schuurmans, Quoc Le, Ed Chi, Sharan Narang,
Aakanksha Chowdhery, and Denny Zhou.
\href{https://arxiv.org/abs/2203.11171}{\textcolor{blue}{Self-Consistency Improves Chain of Thought Reasoning in Language Models}}
\textit{arXiv preprint arXiv:2203.11171}, 2022.

\bibitem{yao2022react}
Shunyu Yao, Jeffrey Zhao, Dian Yu, Nan Du, Izhak Shafran,
Karthik Narasimhan, and Yuan Cao.
\href{https://arxiv.org/abs/2210.03629}{\textcolor{blue}{{ReAct}: Synergizing Reasoning and Acting in Language Models}}
\textit{arXiv preprint arXiv:2210.03629}, 2022.

\bibitem{cobbe2021gsm8k}
Karl Cobbe, Vineet Kosaraju, Mohammad Bavarian, Mark Chen, Heewoo Jun,
Lukasz Kaiser, Matthias Plappert, Jerry Tworek, Jacob Hilton,
Reiichiro Nakano, Christopher Hesse, and John Schulman.
\href{https://arxiv.org/abs/2110.14168}{\textcolor{blue}{Training Verifiers to Solve Math Word Problems}}
\textit{arXiv preprint arXiv:2110.14168}, 2021.

\bibitem{yang2018hotpotqa}
Zhilin Yang, Peng Qi, Saizheng Zhang, Yoshua Bengio, William W. Cohen,
Ruslan Salakhutdinov, and Christopher D. Manning.
\href{https://arxiv.org/abs/1809.09600}{\textcolor{blue}{{HotpotQA}: A Dataset for Diverse, Explainable Multi-hop Question Answering}}
In \textit{Proceedings of EMNLP}, 2018.

\bibitem{hu2021lora}
Edward J. Hu, Yelong Shen, Phillip Wallis, Zeyuan Allen-Zhu, Yuanzhi Li,
Shean Wang, Lu Wang, and Weizhu Chen.
\href{https://arxiv.org/abs/2106.09685}{\textcolor{blue}{{LoRA}: Low-Rank Adaptation of Large Language Models}}
\textit{arXiv preprint arXiv:2106.09685}, 2021.

\bibitem{dettmers2023qlora}
Tim Dettmers, Artidoro Pagnoni, Ari Holtzman, and Luke Zettlemoyer.
\href{https://arxiv.org/abs/2305.14314}{\textcolor{blue}{{QLoRA}: Efficient Finetuning of Quantized {LLMs}}}
\textit{Advances in Neural Information Processing Systems}, 36, 2023.

\bibitem{dror2018hitchhiker}
Rotem Dror, Gili Baumer, Segev Shlomov, and Roi Reichart.
\href{https://aclanthology.org/P18-1128/}{\textcolor{blue}{The Hitchhiker's Guide to Testing Statistical Significance in Natural Language Processing}}
In \textit{Proceedings of ACL}, 2018.

\bibitem{dietterich1998approximate}
Thomas G. Dietterich.
\href{https://doi.org/10.1162/089976698300017197}{\textcolor{blue}{Approximate Statistical Tests for Comparing Supervised Classification Learning Algorithms}}
\textit{Neural Computation}, 10(7):1895--1923, 1998.

\bibitem{yoffe2024debunc}
Luke Yoffe, Alfonso Amayuelas, and William Yang Wang.
\href{https://arxiv.org/abs/2407.06426}{\textcolor{blue}{{DebUnc}: Improving Large Language Model Agent Communication With Uncertainty Metrics}}
\textit{arXiv preprint arXiv:2407.06426}, 2024.

\bibitem{xie2026spark}
Yizhe Xie, Congcong Zhu, Xinyue Zhang, Tianqing Zhu, Dayong Ye,
Minfeng Qi, Huajie Chen, and Wanlei Zhou.
\href{https://arxiv.org/abs/2603.04474}{\textcolor{blue}{From Spark to Fire: Modeling and Mitigating Error Cascades in {LLM}-Based Multi-Agent Collaboration}}
\textit{arXiv preprint arXiv:2603.04474}, 2026.

\end{thebibliography}

\appendix
\renewcommand{\thesection}{\Alph{section}}
\section{Role prompts}
\textbf{Decomposer.} \emph{You are a planning agent. Break the problem into a
short numbered plan of 2--5 concrete steps with any intermediate quantities
needed. Do NOT solve it. Output only the plan.}

\textbf{Solver.} \emph{You are a solving agent. Execute the given plan step by
step on the problem. Show brief working, then end with the line: FINAL
ANSWER: $\langle$answer$\rangle$.}

\textbf{Verifier.} \emph{You are a verification agent. Check the proposed
solution against the original problem. If correct, reply `VERDICT: ACCEPT'.
If you find an error, reply `VERDICT: REJECT', explain the error in one
sentence, and give a corrected line: FINAL ANSWER: $\langle$answer$\rangle$.}

\textbf{Single agent.} \emph{Solve the problem step by step. End with the
line: FINAL ANSWER: $\langle$answer$\rangle$.}

\section{Injection examples}
Number swap: ``Step 1: add \underline{45} and 12.'' $\rightarrow$ ``Step 1:
add \underline{76} and 12.'' (step labels untouched). ``Take the
\underline{8} apples'' $\rightarrow$ ``Take the \underline{3} apples.''
``Compute \underline{250} $\times$ 0.2'' $\rightarrow$ ``Compute
\underline{150} $\times$ 0.2.'' Operation flip: ``\underline{multiply} the
total by 3'' $\rightarrow$ ``\underline{divide} the total by 3.''

\end{document}
